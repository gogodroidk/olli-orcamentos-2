{
  "name": "olli-orcamento-link",
  "private": true,
  "version": "0.1.0",
  "description": "OLLI — link público do cliente (Aprovar/Recusar/WhatsApp) via Cloudflare Worker + Supabase.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.84.0"
  }
}
