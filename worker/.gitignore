# Saída do `wrangler deploy --dry-run --outdir=.dry` (validação local do bundle).
# É artefato de build: nunca versionar.
.dry/
.base/
.wrangler/
# Wrangler-generated binding declarations; regenerate in CI and before deploy.
worker-configuration.d.ts
