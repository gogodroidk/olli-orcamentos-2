# DECISIONS — decisões-chave (registro imutável; nova decisão = nova linha, nunca editar a antiga)

| ID | Decisão | Racional | Data |
| --- | --- | --- | --- |
| D-01 | **Org é CAMADA, não dono dos dados.** Dados do negócio continuam com `user_id` do owner; membros acessam via `donos_visiveis()` na RLS. Sem big-bang para `org_id`. | Zero regressão single-tenant, zero reescrita do `cloudSync` (pull já confia na RLS), superfície de risco mínima. Detalhes em `docs/multi-tenant.md`. | 2026-07-07 |
| D-02 | **`tipo_conta` é derivado**, não coluna: conta é "empresa" se há linha ativa em `organizacao_membros`. | Uma fonte de verdade a menos para dessinchronizar. | 2026-07-07 |
| D-03 | **Webhook Stripe é a fonte da verdade do plano.** Usuário nunca altera plano no banco; 12x é mode=payment com acesso de 12 meses gravado pelo webhook. | Mestre §25.6; evita fraude e estado fantasma. | ciclo v2 |
| D-04 | **Escrita conservadora na Onda 2**: técnico escreve só em `orcamentos`/`agendamentos` (com `criado_por`); `empresa`/`clientes`/catálogo/recibos ficam do dono até a UI pedir. | Não abrir superfície de escrita antes de existir tela que a use. | 2026-07-07 |
| D-05 | **Nunca gatear criar orçamento/recibo/cliente/agenda.** Gate = preview real + CTA; proibido "em breve" e erro seco. | É a alma do grátis e da ativação. | Onda 1 |
| D-06 | **E-mail transacional = Resend** com `mail.olliorcamentos.online` (DNS Hostinger via MCP); mailto: só fallback offline; Gmail API nunca como motor do SaaS. | Mestre §24.2; única dependência humana é a API key. | roadmap v7 |
| D-07 | **Grátis generoso no fluxo, marca OLLI na apresentação** (PDF + link público). Pro remove a marca (`quotes.remove_olli_brand`), desbloqueia templates (grátis fica com 1), relatórios e IA ilimitada. | Resolve a tensão dono×mestre; modelo Canva/Loom. Detalhe em `PLAN_ENTITLEMENTS.md`. | 2026-07-08 |
| D-08 | **Entitlements por CHAVE com limites** (`PLANO_ENTITLEMENTS`) substituem o `Set<Recurso>`; call sites de `<GatePro>` preservados; sistema antigo apagado no mesmo commit. | Mestre §25: limites (`ai.monthly_credits`, `team.max_members`) não cabem em booleano. | 2026-07-08 |
| D-09 | **Backfill de org: lazy, não batch.** `criar_organizacao()` é idempotente; o app chama no primeiro uso de recurso de equipe (convidar/aceitar). Usuário solo NUNCA precisa de org (D-01 garante que ele funciona sem). | Migrar milhares de usuários em batch criaria orgs mortas e risco sem benefício; o mestre pede "org por usuário" — a semântica é atendida on-demand. | 2026-07-08 |
| D-10 | **SÓ 1 APK no fim do ciclo** (regra do dono). Tudo que exige prebuild (expo-location, task-manager, login nativo) vive atrás de flag até lá; APK só sai após teste completo no emulador `olli_phone` (lição Hermes v6). | Cada prebuild é risco; Hermes já quebrou release silenciosamente. | roadmap v7 |
| D-11 | **Sem react-native-reanimated** neste ciclo; motion com Animated core (scale/opacity/translate dão conta). | Evita prebuild novo (D-10). | roadmap v7 |
| D-12 | **Ciclo comercial primeiro** (mestre §4): depois da Onda 2, a prioridade é fechar cliente→orçamento→aprovação no portal→OS mínima→pagamento→recibo ANTES de landing/motion/verticais. Reordenação registrada no roadmap reconciliado. | "Implemente primeiro o que cria um ciclo comercial completo e utilizável." | 2026-07-08 |
| D-13 | **Logo/uploads irão para Supabase Storage com RLS** (hoje URI local + sync limitado) na onda do PDF v2/identidade; chave `storage.limit_mb` já reservada. | Mestre §29; pré-requisito para logo consistente em web+mobile+e-mail. | 2026-07-08 |
| D-14 | Raiz `olliorcamentos.online` = landing estática; app web em subdomínio/`/app` com 301 das rotas antigas; links públicos `/o/<token>` vivem em `link.olliorcamentos.online` e não podem quebrar. | Roadmap v7 Onda 3; SEO + primeira impressão. | roadmap v7 |
| D-15 | **Bundle id / package = `online.olliorcamentos.app`** (era `com.grtech.olliorcamentos`). Trocado ANTES da primeira publicação. | O package do Android é permanente depois da publicação na Play Store. `grtech` é o nome de outra empresa; o produto é da OLLI. Reverse-DNS do único domínio comprovadamente controlado (`olliorcamentos.online`) — não usei `com.olli.*` porque implicaria um `olli.com` que não temos. **Ordem dura:** bundle id → keystore (define o SHA-1) → OAuth client Android (registrado contra o SHA-1). | 2026-07-09 |
| D-16 | **No iOS, assinatura via In-App Purchase da Apple (StoreKit).** Stripe continua no web e no Android. `assinaturas` vira o ponto de reconciliação das duas trilhas. | Guideline 3.1.1: "If you want to unlock features or functionality within your app... you must use in-app purchase." As exceções não cobrem o OLLI: 3.1.3(c) *Enterprise* termina em "Consumer, single user, or family sales must use in-app purchase" — o alvo do OLLI é o autônomo (usuário único); 3.1.3(b) *Multiplatform* só vale "provided those items are also available as in-app purchases within the app"; e 3.1.3(f) *Free Stand-alone* exige ser companion de uma ferramenta **web** paga (VoIP, storage, e-mail, hospedagem) — o app OLLI **é** o produto, offline-first, e é nele que o recurso pago destrava. Custo: 15% (Small Business Program, receita < US$1M/ano). | 2026-07-09 |
