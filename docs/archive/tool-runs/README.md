# Tool run archive

Historical outputs from local automation and inspection tools.

## Folders

- `codex`: Codex app logs that were previously tracked in `.codex`.
- `playwright-mcp`: screenshots, console logs and page snapshots produced by Playwright MCP.

Future tool output should stay ignored unless it becomes intentional release/QA evidence and has a README or manifest.
