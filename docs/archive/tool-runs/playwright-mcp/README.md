# Playwright MCP archive

This folder keeps Playwright MCP screenshots, console logs, and page snapshots that were previously tracked under `.playwright-mcp`.

Future `.playwright-mcp` output is ignored. If a future capture becomes important evidence, move it into a dated archive folder and add a manifest.
