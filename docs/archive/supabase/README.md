# Supabase archive

Historical Supabase local CLI state moved out of `supabase/.temp`.

| File | Why it is here |
| --- | --- |
| `cli-latest.txt` | Previously tracked `supabase/.temp/cli-latest`; kept as historical CLI-version context. Future `.temp` files are ignored. |
