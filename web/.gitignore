# dependencies
node_modules/

# build output
dist/
dev-dist/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / OS
.DS_Store
*.local

# generated PWA dev artifacts
public/registerSW.js

# typescript
*.tsbuildinfo

# NOTE: web/.env is intentionally committed for this project because it only
# contains the PUBLIC Supabase anon key (RLS protects the data). Keep real
# secrets out of .env. Local overrides go in .env*.local (ignored below).
.env*.local
